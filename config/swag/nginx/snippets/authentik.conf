
# Auth with Authentik Outpost for Nginx
# Requires the 'authentik-proxy' (outpost) container reachable as http://authentik-proxy:9000
auth_request /outpost.goauthentik.io/auth/nginx;

location = /outpost.goauthentik.io/auth/nginx {
    internal;
    proxy_pass http://authentik-proxy:9000/outpost.goauthentik.io/auth/nginx;
    proxy_pass_request_body off;
    proxy_set_header Content-Length "";
    proxy_set_header X-Original-URL $scheme://$http_host$request_uri;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $http_host;
}

# Pass Authentik headers to upstream
auth_request_set $authentik_username $upstream_http_x_authentik_username;
auth_request_set $authentik_groups $upstream_http_x_authentik_groups;
proxy_set_header X-Authentik-Username $authentik_username;
proxy_set_header X-Authentik-Groups $authentik_groups;
