
location /authentik/ {
    include /config/nginx/proxy.conf;
    resolver 127.0.0.11 valid=30s;
    set $upstream http://authentik:9000;
    proxy_pass $upstream/;
    sub_filter_once off;
    sub_filter 'href="/' 'href="/authentik/';
    sub_filter 'src="/' 'src="/authentik/';
}
