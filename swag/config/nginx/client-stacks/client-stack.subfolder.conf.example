# swag/config/nginx/client-stacks/client-stack.subfolder.conf.example
#
# Example client stack exposed as a subfolder.
# Copy this file to:
#   swag/config/nginx/client-stacks/my-client.subfolder.conf
#
# This file must be included inside an existing server block.
#
# location = /my-client {
#     return 301 /my-client/;
# }
#
# location ^~ /my-client/ {
      # -> Hardening phase 2 : OIDC
      # include /config/nginx/custom/admin-auth-oidc.conf;
#     include /config/nginx/proxy.conf;
#     include /config/nginx/resolver.conf;
#
#     set $upstream_app my-client-app;
#     set $upstream_port 8080;
#     set $upstream_proto http;
#     proxy_pass $upstream_proto://$upstream_app:$upstream_port;
# }